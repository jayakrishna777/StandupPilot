# StandupPilot Chrome extension

The extension is a vanilla JavaScript Chrome Manifest V3 project. Ticket 01 declares
the Node.js toolchain only; caption observation, stabilization, delivery, attribution,
and the upstream license are implemented in Ticket 02.

Use Node.js 20 or newer and run the extension tests with:

```bash
npm test
```
